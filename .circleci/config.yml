---
version: 2.1
jobs:
  test:
    docker:
      - image: cimg/ruby:2.7.4-browsers
      - image: mongodb
    steps:
      - checkout
      - run:
          name: hello
          command: echo "ohla"

workflows:
  test_build:
    jobs:
      - test
