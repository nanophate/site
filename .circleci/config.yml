---
version: 2.1
jobs:
  test:
    docker:
      - image: cimg/ruby:2.7.4-browsers
      - image: nginx
    steps:
      - checkout
      - run:
          name: hello
          command: printenv
          

workflows:
  test_build:
    jobs:
      - test 
  daily-run-workflow:
    triggers:
      - schedule:
          cron: "1 * * * *"
          filters:
            branches:
              only:
                - server-check
    jobs:
      - test:
          context: 
            - test-line
